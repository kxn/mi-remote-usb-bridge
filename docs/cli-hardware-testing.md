# CLI 真机测试

[运行命令](host-sdk-demo.md)。关闭 GUI 后让 CLI 独占串口。已有绑定短按方向键唤醒，再按住语音键录音；首次配对用 --pair 手动选择，--scan 只发现，--no-connect 观察板子自主恢复。
记录真实按压时间、位置变化及录音开始/结束。约60秒遥控器会主动终止。保留压缩 JSONL、WAV 和日志；试听不能替代协议完整性检查。
